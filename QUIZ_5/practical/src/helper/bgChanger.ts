
// This function checks the pathname and returns a specific background
export default function bgChanger(path: string) {
    // This will check the path and return a string
    switch(path) {
        default:
            return "../bglogin.png"
    }
}